#include<stdio.h>
#define _CRT_SECURE_NO_WARNINGS

struct date
{
	int year;
	int month;
	int day;
};
int dayofyear(struct date b);
int main() {
	struct date a;
	scanf_s("%d %d %d",&a.year,&a.month,&a.day);
	printf("%d", dayofyear(a));
	return 0;
}
int dayofyear(struct date c) {
	int a[12] = { 31,28,31,30,31,30,31,31,30,31,30,31 };//平年
	int b[12] = { 31,29,31,30,31,30,31,31,30,31,30,31 };//闰年
	int i,sum=0;
	if (c.year%4==0 && c.year%100!=0)
	{
		for (i = 0; i < c.month-1; i++) {
			sum += b[i];
		}
		sum += c.day;
	}
	else
	{
		for (i = 0; i < c.month-1; i++) {
			sum += a[i];
		}
		sum += c.day;
	}
	return sum;
}
